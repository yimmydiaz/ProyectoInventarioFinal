@model IEnumerable<Inventario.GUI.Models.Asociar.ModeloAsociar>

@{
    ViewBag.Title = "Index";
}

<h2>Lista De Asociados</h2>
@{int contador = 1; }
<p>
    @Html.ActionLink("Crear Asociado", "Create", new { }, new { @class = "btn btn-success" })
</p>
<input type="text" name="txtFiltro" id="txtFiltro" />
<button type="submit" class="btn btn-info "> Filtrar</button>
<table class="table">
    <tr>
        <th>Nº</th>

        <th>
            @Html.DisplayNameFor(model => model.NombrePersona)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ubicacion)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@contador</td>
            
            <td>
                @Html.DisplayFor(modelItem => item.NombrePersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ubicacion)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.Id }, new { @class = "btn btn-warning" }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
        contador++;
    }

</table>
@Html.Partial("_Footer_pageList")